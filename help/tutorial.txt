TUTORIAL INSTALACAO FEDORA 17 / CENTOS 6

+ Virtualenv
    # virtualenv --system-site-packages GED

+ Python 2.7.3
	# yum install python-devel python-setuptools

+ Django 1.3.1
	# wget http://www.djangoproject.com/m/releases/1.3/Django-1.3.1.tar.gz
	# tar xzvf Django-1.3.1.tar.gz
	# cd Django-1.3.1
	# python setup.py install 

+ Apache 2.2
	# yum install httpd mod_wsgi

+ PosgreSQL 8.4.12
	# yum install postgresql postgresql-server postgresql-contrib
	# service postgresql initdb
	# service postgresql start
	# su postgres
	$(bash) psql
	$(postgres) alter user postgres with password 'shift@ged';
	$(postgres) \q
	$(bash) exit
	# vim /var/lib/pgsql/data/pg_hba.conf
		original: # "local" is for Unix domain socket connections only
			  local   all         all                               ident
		novo	: # "local" is for Unix domain socket connections only
			  local   all         all                               md5
	# service postgresql restart

## Crontab

	crontab -e
	adicionar: * * * * * python /project/path/manage.py cron

##Daemon
	# pip install zdaemon
	Ex.: zdaemon -p "/home/webapps/GED/bin/python manage.py servidor" -z /home/webapps/GED/PyProject_GED/ -l /home/webapps/GED/logs/servidor.log -d start

